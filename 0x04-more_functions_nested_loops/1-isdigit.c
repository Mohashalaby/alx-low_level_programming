#include "main.h"

/**
 * _isdigit - function to check digits .
 * @c: takes an digit to be chicks
 * Return: return 0 success return 1 not
 */
int _isdigit(int c)
{
	if (c >= 48 && c <= 57)
	return (0);
else
return (1);
}
