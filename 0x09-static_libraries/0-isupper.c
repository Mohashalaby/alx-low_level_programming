#include "main.h"

/**
* _isupper - check the uppercase
* @c: input alphabet
* Return: 1 if upper case 0 if not
*/
int _isupper(int c)
{

	if (c >= 'A' && c <= 'Z')


	return (1);
else

	return (0);
}
